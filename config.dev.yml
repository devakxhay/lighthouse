# config.dev.yaml
ca:
  dir: ./dev-ca

dirs:
  certs: ./dev-data/certs
  envs:  ./dev-data/envs
  units: ./dev-data/units   ← writes unit files here, doesn't load them
  data:  ./dev-data

nginx:
  sites_available: ./dev-data/nginx
  sites_enabled:   ./dev-data/nginx-enabled

server:
  port: 9000